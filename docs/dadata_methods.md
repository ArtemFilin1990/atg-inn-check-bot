# Методы DaData, используемые в проекте

Документ фиксирует только **подтверждённые в коде репозитория** методы и параметры API DaData.

## Основной сценарий в проекте

1. Пользователь отправляет ИНН (10 или 12 цифр).
2. Бот делает **один** запрос в DaData `findById/party`.
3. Полученный объект сохраняется в локальный TTL-кеш.
4. Inline-кнопки (`Суды`, `Оборот`, `Долги`, `Штрафы`, `Контакты`, `Учредители`, `Реквизиты`) раскрывают поля **из кеша**, без повторных API-вызовов.

## `findById/party`

- URL: `https://suggestions.dadata.ru/suggestions/api/4_1/rs/findById/party`
- Назначение: получение карточки компании по ИНН.
- Используется в `find_by_id_party()` и в `_lookup_and_reply()`.

### Параметры (подтверждённые)

- `query: str` — ИНН (обязательный).
- `count: int` — число результатов (обязательный, `> 0`).
- `branch_type: str | None` — опционально.
- `kpp: str | None` — опционально.
- `type` (в коде аргумент `entity_type`) — опционально.

### Валидация/ограничения в проекте

- На уровне бота поддерживается только ИНН.
- Пустой `query` запрещён.
- `count <= 0` запрещён.

## Общие правила вызова DaData в проекте

- Авторизация: заголовок `Authorization: Token <DADATA_API_KEY>`.
- Таймаут HTTP-запроса: `10s`.
- Кеширование HTTP-ответа DaData в клиенте: TTL cache, до `512` записей, TTL `900s`.
- Кеширование карточки для callback-кнопок в боте: локальный TTL-кеш `600s`.
- Формат ответа: ожидается JSON-объект (`dict`).

## Ограничения верификации в текущем окружении

Прямой доступ к `dadata.ru` и `github.com` из контейнера ограничен proxy (HTTP 403), поэтому документ основан на исходниках и тестах этого репозитория, без добавления неподтверждённых полей/параметров.
